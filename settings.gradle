pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = ("Sliide")
include(':app',
        ':core:base',
        ':core:components',
        ':core:navigation',
        ':core:remote',
        ':features:main',
        ':features:users'
)